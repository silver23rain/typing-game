import { testEnvironment } from '../../../jest.config';
import StartGameButton from '../StartGameButton';

describe('StartGameButton 컴포넌트', () => {
	test('시작 버튼을 누르면 텍스트는 "초기화"로 변경된다."', () => {});
	test('초기화 버튼을 누르면 버튼 텍스트는 "시작"으로 변경된다.', () => {});
});
